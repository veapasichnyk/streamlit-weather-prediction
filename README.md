# 🌧️ Aussie Rain Prediction — Streamlit App

### Прогнозування ймовірності дощу в Австралії на основі ML-моделі

---

## 🚀 Demo

🔗 **Live App:** (https://app-weather-prediction-tomorrow.streamlit.app/)  
📦 **Model Pipeline:** `aussie_rain_pipeline_compressed`

---

## 📖 Опис проєкту

Це веб-додаток, створений на **Streamlit**, який прогнозує, чи буде дощ завтра на основі метеорологічних даних.

Модель будується на основі **RandomForestClassifier** та повного **ML pipeline**, що включає:

- імпутацію пропусків  
- масштабування  
- one-hot encoding  
- модель класифікації  

Датасет: **WeatherAUS** — 10 років щоденних спостережень, понад 140 000 записів.

---

## 📁 Структура репозиторію

```
streamlit-aussie-rain/
│── app.py                                    # Streamlit-додаток
│── aussie_rain_pipeline_compressed.joblib    # Збережений ML-пайплайн
│── requirements.txt                          # Залежності
└── README.md                                 # Опис проєкту
```

---

## 🧠 Що всередині моделі?

Модель створена за допомогою **Scikit-Learn Pipeline**:

```python
Pipeline(steps=[
    ('preprocessor', ColumnTransformer(
        transformers=[
            ('num', Pipeline([
                ('imputer', SimpleImputer(strategy='mean')),
                ('scaler', MinMaxScaler())
            ]), numeric_cols),

            ('cat', Pipeline([
                ('imputer', SimpleImputer(strategy='constant', fill_value='missing')),
                ('onehot', OneHotEncoder(sparse_output=False, handle_unknown='ignore'))
            ]), categorical_cols)
        ]
    )),
    ('classifier', RandomForestClassifier(
        n_estimators=200,
        max_depth=None,
        random_state=42
    ))
])
```

🔹 **Перевага пайплайна:** повна автоматизація препроцесингу при передбаченні.

---

## 🖼️ Функціонал додатка

- Ввід параметрів погоди: температура, вологість, тиск, вітер, хмарність  
- Вибір локації та умов сьогоднішнього дня  
- Автоматична обробка введених даних у пайплайні  
- Прогноз:
  - **Yes** — буде дощ  
  - **No** — дощу не буде  
- Виведення ймовірності у відсотках  
- Відображення введених даних  

---

## ▶️ Як запустити локально

### 1. Клонувати репозиторій
```bash
git clone https://github.com/veapasichnyk/streamlit-aussie-rain.git
cd streamlit-aussie-rain
```

### 2. Встановити залежності
```bash
pip install -r requirements.txt
```

### 3. Запустити Streamlit
```bash
streamlit run app.py
```

---

## 📦 Технології

- Python 3.10+  
- Streamlit  
- Pandas  
- NumPy  
- Scikit-learn  
- Joblib  

---

## 💡 Автор

**Вероніка Пасічник**  
· Weather Modelling · Streamlit Deployment

---

## 📜 Ліцензія

Вільний для використання у навчальних та демонстраційних цілях.